The purpose of this example is to show the execution of the program for simple cases, whose complexity is O(N). If the code in the file is accepted by the chosen regex, such as "A(BCE|BDE)+F", it is expected a successful result. Otherwise, the result is expected to be unsuccessful.
