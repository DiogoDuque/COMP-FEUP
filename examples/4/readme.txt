The purpose of this example is to show the execution of the program for cases where there is more than one function and multiple files. If the code in the files is accepted by the chosen regex, such as "AB+CD*EF", it is expected a successful result. Otherwise, the result is expected to be unsuccessful.
